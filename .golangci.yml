version: "2"

linters:
  default: standard
  enable:
    - gosec
    - gocritic
    - revive
    - lll
    - nolintlint

  settings:
    lll:
      line-length: 180
    gosec:
      excludes:
        - G304
    revive:
      rules:
        - name: exported
          disabled: true

  exclusions:
    presets:
      - std-error-handling
      - common-false-positives
    rules:
      - path: _test\.go
        linters:
          - gosec

run:
  go: "1.22"
  concurrency: 2
